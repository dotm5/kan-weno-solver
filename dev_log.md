# 开发日志

## 2026-03-07 更新（Workflow 文档同步）
**主题：README / DevLog 同步为配置化工作流与门控诊断流程**

1. **文档重构为 YAML 驱动工作流**
   - `README.md` 已改为“配置优先”的运行说明，统一入口为：
     - `python -m data.generate [--config ...]`
     - `python train.py [--config ...]`
     - `python evaluate.py [--config ...]`
     - `python rollout.py [--config ...]`
   - 明确默认配置文件：`config/default_config.yaml`，并说明用户配置递归覆盖机制。

2. **新增/补全文档的关键流程**
   - 增加门控消融流程说明：`original / raw_gate_only / gate_open / soft_mask`。
   - 增加调试流程说明：`logging.debug_rollout`、`logging.debug_t_start`。
   - 增加评估图说明：上图 L2 误差、下图门控行为与修正量曲线。

3. **训练与推理尺度一致性文档化**
   - 在文档中补充 `TargetAffineScaler` 的训练归一化与评估反归一化路径。
   - 在 checkpoint 字段说明中加入 `target_scaler_state` 与 `config` 快照，便于复现实验。

4. **数据生成流程文档化**
   - 明确多会话样本采集（multi-session）和滚动进度条 + ETA 的使用方式。
   - 文档中保留最小可运行命令与配置覆盖示例，便于快速复现。

---

## 2026-03-07 更新（补充）
**主题：Target Affine Scaling（目标仿射归一化）上线**

1. **问题背景**
   - 训练目标（物理修正量）量级约为 `1e-5`，导致梯度信号过弱，门控网络（Gate）容易出现学习不充分或近似失活现象。

2. **核心改动（train/eval 端到端对齐）**
   - 在 `train.py` 中新增 `TargetAffineScaler`，按训练集统计量执行：
     `y_scaled = (y - mean) / std`。
   - 模型训练目标改为标准化后的 `y_scaled`，显著改善小量级目标导致的优化困难。
   - 在 `evaluate.py` 中同步引入反变换：
     `y_phys = y_scaled * std + mean`，保证推理输出仍为物理量级修正值。

3. **Checkpoint 兼容与存储**
   - 新增 `target_scaler_state` 持久化字段（`mean/std`）并随模型一同保存。
   - 评估阶段对旧模型保持兼容：若缺少 `target_scaler_state`，自动回退为 identity 并打印警告。

4. **指标与训练监控优化**
   - 训练日志中同时报告：
     - 归一化空间损失（用于调度器稳定优化）
     - 物理空间加权误差（用于可解释性评估）
   - 兼顾优化稳定性与物理意义监控。

5. **预期收益**
   - 提升梯度有效幅值，缓解小目标量级带来的数值精度问题。
   - 促进 Gate 在激波/平滑区域学习到更有效的区分策略。

---

## 2026-03-07 更新
**主题：门控网络修复 + 数据生成流程升级（多会话）**

1. **Gate 学习特征升级（v4.2）**
   - 原门控网络在训练中出现接近常量输出（约 99% 一侧分裂）的饱和问题。
   - 新版在 `kan/model.py` 中引入了更丰富的门控输入与派生梯度特征，并加入 temperature 缓和 sigmoid 饱和。
   - 目标是提升 gate 在激波/平滑区域间的分辨能力，避免早期塌缩。

2. **时间戳嵌入 + 梯度特征扩展**
   - 数据特征从原先的 3 维物理量扩展到 7 维：
     `[u_x, |u_x|, |u_xx|, grad_var, dt, sin(t), cos(t)]`。
   - `data/generate.py`、`train.py`、`evaluate.py` 已对齐该输入管线。

3. **单会话生成改为多会话生成（multi-session）**
   - 数据集不再仅依赖单次采样逻辑，而是通过多个 rollout session 收集样本。
   - 支持 `--num-sessions`、`--sample-stride` 等参数，提升时序覆盖与样本多样性。

4. **新增滚动进度条与 ETA**
   - `data.generate` 在生成过程中显示实时进度条、已完成点数、会话数、耗时与 ETA。
   - 便于长时间生成任务的监控和运行评估。

5. **兼容性与稳健性**
   - `HybridScaler` 增强为可处理可变维度物理特征，并保持对旧 checkpoint/scaler 状态的兼容加载。
   - `train.py` 由数据自动推断 `phys_dim`，减少硬编码风险。

---

## 2026-02-15 更新
**Git 提交哈希值：** `4b5914c`

1. 实验观察表明，在时间步长 $t=0.1$ 的数据集上计算所得结果的参考价值有限。该现象可能归因于时间尺度过短所诱发的高阶误差项，从而导致模型学习过程的无效性与收敛障碍。
2. 后续研究计划：集成多点采样策略、物理梯度加权机制以及多时间尺度采样框架，以系统性地缓解前述问题并提升模型的鲁棒性。
3. [后续补充]

---

## 2026-02-16 更新
当前研究焦点：将 WENO5 方案中 $\beta_k$ 平滑指标进一步嵌入门控函数的学习特征表示中，以实现对解的间断结构与振荡行为的精确辨识，从而增强模型的泛化性能。拟于 Burgers 方程基准问题上执行最小化验证实验，以评估该增强框架的表达能力及其在非线性偏微分方程求解中的适用性。

---

## 2026-02-17 更新
**重构与模块化：**

1. **架构解耦**：完成了核心代码的大规模重构，建立了 `kan` 独立包，包含 `model` (模型定义)、`scalers` (物理归一化器) 和 `losses` (物理一致性损失函数) 等子模块。
2. **结构优化**：
   - 建立了 `solvers/` 目录存放 WENO5 数值引擎。
   - 建立了 `data/` 目录统一管理数据集生成逻辑。
   - 建立了 `tests/` 目录进行基准测试（如 WENO5 收敛性验证）。
3. **流程规范化**：整合了训练入口 `train.py` 和评估入口 `evaluate.py`，显著降低了顶级目录的代码冗余，提升了项目的可读性和维护性。
4. **环境清理**：移除了过时的模型权重文件 (.pth) 和实验过程图表 (.png)，确保工作区整洁。
